[project]
name = "minecraft-server-auto-backup"
version = "0.1.0"
description = "Minecraft Server Auto Backup via SFTP"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "onedrive-upload>=1.0.0",
    "paramiko>=5.0.0",
]

[dependency-groups]
dev = [
    "dotenv>=0.9.9",
]
